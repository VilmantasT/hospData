let body = document.getElementById("body")
let table = document.createElement("table")

let th = document.createElement("tr")
th.innerHTML = "<tr></tr>"

table.appendChild()

allData['ziniarastis'].forEach(data => {
  console.log(data)
})
